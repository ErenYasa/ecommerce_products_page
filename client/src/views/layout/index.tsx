import Listing from "../listing";
import { LayoutStyle } from "./styled";

export default function Layout() {
    return (
        <LayoutStyle>
            <Listing />
        </LayoutStyle>
    );
}
